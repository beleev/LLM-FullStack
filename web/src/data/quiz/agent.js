// 阶段 6 · llm_agent 自测题: 每章 3 题, 考取舍和"为什么", 错误选项是常见误解
export default {
  agent: [
    {
      q: '一个 Agent 产品的可靠性, 主要来自哪里?',
      options: ['更聪明的模型', '更长的 system prompt', 'loop 周围的确定性系统: 权限、校验、hooks、持久化、上下文管理', '更复杂的 while 循环'],
      answer: 2,
      why: 'loop 本身只有十几行; 能否拒绝危险动作、恢复状态、压缩上下文并留下审计记录, 都发生在模型之外。',
    },
    {
      q: '教学版为什么默认用确定性的 RuleBasedLLM, 而不是直接接真实 API?',
      options: ['要看的是模型输出如何被结构化、检查、执行和记录, 确定性模型让每一步都可断言', '真实 API 不支持工具调用', '规则模型效果更好', '为了少写代码'],
      answer: 0,
      why: 'harness 对模型的全部依赖只是 LLM 协议的 next(); 换成 ClaudeLLM 时工具、权限、hook、持久化一行不用改 (m15)。',
    },
    {
      q: '下面哪件事应该交给模型, 而不是 harness?',
      options: ['判断一条 shell 命令是否命中 deny 规则', '校验工具参数是否符合 JSON Schema', '把密钥从工具输出里脱敏', '决定下一步调用哪个工具、传什么参数'],
      answer: 3,
      why: '模型负责"选动作" (概率性), harness 负责"执行边界" (确定性)。安全相关的判断不能依赖模型听话。',
    },
  ],
  'agent-loop': [
    {
      q: '工具执行时抛了异常 (或参数不合 schema), loop 应该怎么处理?',
      options: ['终止 loop 并把异常抛给用户', '变成 is_error=true 的 tool_result 回填给模型', '静默忽略, 继续下一轮', '自动重试同一个调用直到成功'],
      answer: 1,
      why: '错误是观察不是异常: 模型看到 INVALID_ARGS 之类的结果, 下一轮可以自己改参数; 丢掉结果还会破坏 tool_use/tool_result 配对。',
    },
    {
      q: '一个 assistant turn 里发了 3 个 tool_use, 它们的 tool_result 应该放在哪里?',
      options: ['三条独立的 user 消息', '同一条 user 消息里, 各自用 tool_use_id 指回', '拼成一段文本放进 system 消息', '放进下一条 assistant 消息'],
      answer: 1,
      why: '这是 Messages API 的硬性要求: tool_result 必须紧跟同 id 的 tool_use, 同一轮的结果放同一条 user 消息。',
    },
    {
      q: '为什么 agent 会话越长, 每多一轮越贵?',
      options: ['模型越来越慢', '工具越来越多', 'JSONL 文件越来越大', '每次调用模型都要重发整个上下文, 累计 input token 随轮数近似平方增长'],
      answer: 3,
      why: '模型是无状态的, 状态全在 messages 里。这正是并行工具调用 (少几轮) 和上下文压缩 (每轮更短) 值钱的原因。',
    },
  ],
  'agent-tools-permissions': [
    {
      q: '权限规则同时命中一条 allow 和一条 deny, 结果是什么?',
      options: ['deny 优先: 裁决顺序固定为 deny → ask → allow → 模式兜底', '看哪条规则写在前面', 'allow 优先, 因为更具体', '交给模型决定'],
      answer: 0,
      why: '顺序就是优先级。广义的拒绝必须压过狭义的允许, 否则一条宽松的 allow 就能把安全网捅穿。',
    },
    {
      q: 'deny 规则 "*rm -rf*" 经过归一化后挡住了 `RM  -r -f /`。下面哪句话是对的?',
      options: ['现在删除类命令都被挡住了', '再多加几条正则就能穷尽', '归一化只堵住最廉价的绕过; find / -delete、rm --recursive 这类绕法有无穷多', '黑名单比 allowlist 更安全'],
      answer: 2,
      why: '黑名单是在枚举"坏", 而坏是无穷的。真实系统靠默认拒绝的 allowlist + 命令解析 + OS 沙箱, deny 规则只是最后一道便宜的网。',
    },
    {
      q: 'default 模式下命中 ask、但当前没有人可问 (headless 运行), 应该怎么办?',
      options: ['放行, 免得任务卡住', '交给模型自己判断风险', '拒绝 (fail closed)', '等待直到有人上线'],
      answer: 2,
      why: '无法确认就拒绝。"没人可问时放行"等于把 ask 规则变成 allow 规则。',
    },
  ],
  'agent-context-memory': [
    {
      q: '分词器只认 [a-z0-9]+ 时, 用中文查询检索会怎样?',
      options: ['所有文档得分为 0: 查询向量是空的', '得分偏低但排序仍然正确', '只有中文文档能被检索到', '程序报错'],
      answer: 0,
      why: '中文整句被正则丢光。字符 bigram 不依赖词典就能让中文产生可匹配的 token。',
    },
    {
      q: '关键词计数把一篇凑满 how / the / model 的 FAQ 排到了第一。TF-IDF 是怎么修正的?',
      options: ['删掉所有停用词', '只看文档标题', '按文档长度排序', '让出现在几乎每篇文档里的词 idf 趋近 0, 罕见词主导排序'],
      answer: 3,
      why: 'idf = ln(1+(N−df+0.5)/(df+0.5)): df 越大权重越小。fragmentation 的 idf 是 1.79, the 只有 0.33。',
    },
    {
      q: '为什么记忆用 Markdown 文件、并且每轮现查现拼, 而不是写进 transcript?',
      options: ['Markdown 比数据库快', '文件可读可改可进版本库; 不写进 transcript, 文件改了下一轮立刻生效', '模型只认 Markdown', '这样可以不占上下文'],
      answer: 1,
      why: '透明 + 可编辑是文件记忆的核心价值。它仍然占上下文, 所以要按当前 prompt 检索, 只取相关的几条。',
    },
  ],
  'agent-extensibility': [
    {
      q: '旧顺序是"权限门 → PreToolUse hook → 执行"。一个把 calculator 改写成 shell rm -rf / 的 hook 会造成什么?',
      options: ['没影响, deny 规则仍会拦住', 'deny 规则被绕过: 门检查的是改写前的 calculator, 改写后的调用没人再看', 'hook 会报错', '模型会拒绝执行'],
      answer: 1,
      why: '检查之后还能被修改的东西等于没检查 (TOCTOU)。修复: hook → 权限门 → 执行, 门评估最终要执行的那个调用。',
    },
    {
      q: '装了 40 个 skill, 每个正文 3000 token。渐进式披露常驻上下文的大约是多少?',
      options: ['约 120K (全部正文)', '0, skill 不占上下文', '约 3K (一个正文)', '约 1.2K (40 条 name + description), 用到哪个再加载哪个的正文'],
      answer: 3,
      why: '常驻的只有目录。代价是模型只凭一行 description 判断要不要加载 —— description 写得含糊, skill 就不会被触发。',
    },
    {
      q: '"提交前必须跑 lint"这条团队规范, 写成 hook 而不是写进 system prompt, 好处是什么?',
      options: ['零 token 成本且 100% 执行: 确定性代码不依赖模型"记得照做"', 'hook 能让模型更聪明', 'hook 可以绕过权限门', 'system prompt 不支持中文'],
      answer: 0,
      why: '能用确定性代码解决的就不要消耗上下文去"求"模型。但 hook 不是提权通道, 改写后的调用照样过门。',
    },
  ],
  'agent-state-subagents': [
    {
      q: 'resume 一个之前以 bypass_permissions 运行过的会话, 权限应该怎样?',
      options: ['继承上次的 bypass_permissions', '全部工具禁用', '由新会话重新建立: 恢复的只是消息, 不是信任', '由 transcript 里的最后一条消息决定'],
      answer: 2,
      why: 'transcript 是数据, 权限是当前运行环境的决定。把权限写进可被恢复的状态, 等于让一个文件能给自己提权。',
    },
    {
      q: '上下文被压缩之后, JSONL 文件发生了什么?',
      options: ['旧消息被删除以节省空间', '文件被整体重写', '只追加一条 compact_boundary: load() 读回压缩视图, load_all() 仍能读到全量', '另存为新文件'],
      answer: 2,
      why: '仅追加 = 审计日志永不丢; 同时 resume 出来的会话确实变小了。两个需求用一条边界记录同时满足。',
    },
    {
      q: '子智能体读了 20 篇文档, 父级上下文里会多出什么?',
      options: ['只有一条摘要 tool_result; 子 transcript 落盘可审计但不回流', '20 篇文档的全文', '子级的完整 transcript', '什么都没有'],
      answer: 0,
      why: '隔离的意义就是不让子任务的冗长轨迹污染父级上下文、也不让父级每一轮为它们重复付 token。',
    },
  ],
  'agent-full-loop': [
    {
      q: 'full_loop 里内置工具、MCP 工具、子智能体都以同一个 Tool 接口接入, 最大的好处是?',
      options: ['代码更短', '运行更快', '模型更容易记住工具名', '所有动作走同一条执行面: 同一个参数校验、权限门、hook 和审计, 没有后门'],
      answer: 3,
      why: '统一执行面意味着安全策略只需要在一处正确。任何绕开它的"特殊通道"都会成为漏洞。',
    },
    {
      q: '从 m01 到 m14 不断加机制, Agent.run 的主循环改了多少?',
      options: ['每个模块都要重写 loop', '基本不变: 新机制都是 loop 周围可插拔的零件', '循环被拆成了多个线程', '改成了状态机'],
      answer: 1,
      why: '这就是 harness 设计的目标: loop 薄而稳定, 工具 / 权限 / hooks / 记忆 / 存储都是可替换的构件。',
    },
    {
      q: 'full_loop 同时有权限门、hook 拦截和"ShellTool 只模拟不执行"三层。为什么不只留一层?',
      options: ['历史遗留', '纵深防御: 每层都假设别的层会失守, 任何单层都有已知的绕过方式', '为了演示更多 API', '三层的判断逻辑完全相同, 互为备份'],
      answer: 1,
      why: '字符串规则会被绕过, hook 可能有 bug, 模型可能被注入。各层检查的维度不同, 叠起来才可靠。',
    },
  ],
  'agent-mcp': [
    {
      q: 'MCP server 执行工具时内部抛了异常, 应该怎么回?',
      options: ['JSON-RPC error 字段', '不回复, 让 client 超时', '关闭连接', '正常的 result, 其中 isError=true'],
      answer: 3,
      why: '工具失败是"正常结果", 会变成 is_error 的 tool_result 让模型换个做法; error 字段留给协议层问题 (如 -32601 方法不存在)。',
    },
    {
      q: '为什么要给 MCP 工具名加 mcp__<server>__ 前缀?',
      options: ['防止与内置工具重名, 并且一条 glob 规则 mcp__weather__* 就能管住整个 server', '协议强制要求', '让模型知道它更慢', '加密工具名'],
      answer: 0,
      why: '命名空间既解决冲突也服务于权限管理。发给 server 时前缀会被剥掉。',
    },
    {
      q: '一个 MCP server 在工具注解里自报 readOnlyHint=true。harness 应该?',
      options: ['信任它, 按只读低风险放行', '拒绝连接这个 server', '不信: 第三方自报的注解不可信, 一律按 high 风险走规则 / 审批, 输出按不可信数据处理', '交给模型判断'],
      answer: 2,
      why: '协议解决"怎么接进来", 不解决"能不能信"。恶意 server 当然会说自己只读。',
    },
  ],
  'agent-planning': [
    {
      q: 'plan 模式如果只是 system prompt 里的一句"请先不要修改文件", 问题在哪?',
      options: ['没问题, 模型会遵守', 'prompt 太短', '那只是请求: 模型 (或注入它的文档) 可以不听; 由权限门强制只读才是保证', '会消耗太多 token'],
      answer: 2,
      why: '"只读"由 harness 强制, 不靠模型自觉: plan 模式下非只读工具一律 DENY, 连 allow 规则都不能放行。',
    },
    {
      q: '为什么 todo_write 每次整表覆写, 而不是增量修改某一项?',
      options: ['模型每次都要重新面对完整计划; 状态单一来源, 不会和增量 patch 错位', '实现简单而已', '为了节省 token', 'JSON 不支持局部更新'],
      answer: 0,
      why: '计划是留在上下文里的显式状态。整表覆写让"当前计划"永远只有一个权威版本。',
    },
    {
      q: '3 个互不依赖、各耗时 0.2s 的工具调用放进同一个 assistant turn, 省下的是什么?',
      options: ['只省了工具执行时间', '什么也没省, 审批也并行了', '只省了 token', '工具执行时间 (≈0.2s 而不是 0.6s) 加上两次模型往返及其重发的上下文'],
      answer: 3,
      why: '并行执行让耗时 ≈ 最慢的一个; 少两轮模型调用则省掉两次"重发整个上下文"。授权仍然串行, 审批弹窗不能并发。',
    },
  ],
  'agent-orchestrator': [
    {
      q: '多智能体 (lead + workers) 买到的主要是什么?',
      options: ['更低的总 token 花费', '并行的墙钟时间, 和不被原文淹没的 lead 上下文', '更高的单步准确率', '更简单的代码'],
      answer: 1,
      why: 'm11 demo: lead 峰值上下文 247 vs 单 agent 375, 但总输入 770 vs 413。Anthropic 报告多智能体约为聊天的 15× token。',
    },
    {
      q: '实现并行扇出需要给 agent loop 加一套新的调度器吗?',
      options: ['需要, 要写任务队列', '不需要: delegate 是普通工具, lead 同一轮发多个 tool_use, loop 原有的并行执行就是扇出', '需要, 要用多进程', '需要专门的消息总线'],
      answer: 1,
      why: '好的抽象会复用: 并行工具调用 + 一个会新建子 agent 的工具 = orchestrator–workers。',
    },
    {
      q: '哪种任务最不适合拆给多个 worker?',
      options: ['互相独立的多路资料调研', '对多个文档分别做摘要', '多个城市的天气查询', '子任务之间强依赖、需要共享同一份不断变化的上下文 (例如改同一个文件)'],
      answer: 3,
      why: 'worker 之间上下文隔离, 只能通过摘要沟通。强耦合任务拆开后信息损失大, 还要为重复重建上下文付费。',
    },
  ],
  'agent-guardrails': [
    {
      q: '把工具输出包进 <untrusted_data> 并标记注入特征, 这一层防御的性质是?',
      options: ['概率性的: 降低模型上当的概率, 但正则挡不住改写的注入, 模型也可能不听', '确定性的: 包了就安全', '没用', '会让模型无法读取内容'],
      answer: 0,
      why: '它是在"劝"模型。必须有至少一层不依赖模型听话的确定性防线兜底。',
    },
    {
      q: '污点规则 (tainted) 做了什么?',
      options: ['删掉可疑的工具输出', '禁止读取任何网页', '本轮上下文混入不可信数据后, 高风险工具一律拒绝, 直到下一条真正的用户指令', '让模型重新生成回答'],
      answer: 2,
      why: '这是切断 lethal trifecta (私有数据 + 不可信内容 + 对外通道) 的确定性做法: 模型上当了也无法得手。',
    },
    {
      q: '路径围栏为什么必须"先 resolve 再判断", 而不是检查拼接后的字符串是否以 root 开头?',
      options: ['resolve 更快', '字符串比较不支持中文路径', '/work/../etc/passwd 以 /work 开头却早已逃出沙箱; 符号链接同理', '没区别, 只是风格'],
      answer: 2,
      why: '检查必须作用在真实路径上。先展开 .. 和 symlink, 再 is_relative_to(root)。',
    },
  ],
  'agent-evals': [
    {
      q: '单次成功率 p = 0.9, 连续 8 次全对 (pass^8) 的概率约为?',
      options: ['约 43%', '90%', '约 100%', '约 72%'],
      answer: 0,
      why: '0.9⁸ ≈ 0.43。可靠性是指数衰减的: 要 pass^8 ≥ 90%, 单次得到 98.7%。',
    },
    {
      q: '一个会自动退款、发邮件的客服 agent, 上线前应该盯哪个指标?',
      options: ['pass@k: 多试几次总能成', '平均回复长度', '只看一次演示的效果', 'pass^k: 动作不可撤销、没人复核, 用户感受到的是"每次都对"的概率'],
      answer: 3,
      why: 'pass@k 适合有验证器、可以重试挑最优的场景 (写代码跑测试); 面向用户的 agent 没有重来的机会。',
    },
    {
      q: 'grader 为什么检查环境终态 (笔记写了没、危险命令跑了没), 而不是匹配模型的回答文本?',
      options: ['文本匹配太慢', '模型说"我已经完成了"不等于真的完成; 反过来措辞不同也可能做对了', '为了支持多语言', '终态更容易造假'],
      answer: 1,
      why: '以终态判分 + 轨迹检查 (没调用禁用工具、没超调用预算): 评的是发生了什么, 不是说了什么。',
    },
  ],
  'agent-context-engineering': [
    {
      q: '上下文超预算时, 为什么先"清旧工具结果", 不够再"模型摘要"?',
      options: ['摘要效果更差', '由便宜到贵: 清理零模型开销、不破坏配对结构, 而且被清掉的内容可以再调一次工具取回', '清理能腾出更多空间', '摘要会触发权限检查'],
      answer: 1,
      why: '工具结果最胖也最快过时; tool_use 块还在, 模型知道去哪重新取。摘要有损且要多花一次模型调用。',
    },
    {
      q: '硬截断 (头尾保留、中间每条留 32 字符) 最大的问题是?',
      options: ['实现复杂', '太慢', '会多花一次模型调用', '丢的是对话本身 (中途的决定), 还会破坏 tool_use/tool_result 配对, 而且没处可取回'],
      answer: 3,
      why: '它是反例基线: 便宜, 但语义和结构都会坏。而且只裁视图, 历史和 resume 都不会变小。',
    },
    {
      q: '希望一条结论在摘要压缩之后、甚至新会话里仍然可用, 最稳妥的做法是?',
      options: ['用 memory 工具写进 /memories: 状态在窗口之外, 任何压缩都碰不到, 新会话再读回来', '把它重复写很多遍', '调大上下文预算', '写进 system prompt 并祈祷'],
      answer: 0,
      why: '不进窗口的状态才不怕压缩。PreCompact hook 能点名保留要点, 但跨会话只能靠文件。',
    },
  ],
}
